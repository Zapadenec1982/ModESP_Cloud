# DEPRECATED — Replaced by mosquitto-go-auth SQL ACL in Phase 4.
# This file is no longer used. ACL is now computed dynamically from PostgreSQL.
# Kept for reference only. See mosquitto.conf auth_opt_pg_aclquery.
#
# To rollback to static ACL:
# 1. Restore mosquitto.conf from backup (remove auth_plugin directives)
# 2. Uncomment acl_file /etc/mosquitto/acl.conf
# 3. Restore this file with actual device entries
# 4. systemctl restart mosquitto
